{
  "name": "bunkerapp",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:start": "supabase start",
    "db:migrate": "supabase db push",
    "db:seed:users": "node --env-file=.env.local scripts/seed-users.mjs",
    "db:seed:data": "node --env-file=.env.local scripts/seed-data.mjs",
    "db:seed": "pnpm db:seed:users && pnpm db:seed:data"
  },
  "devDependencies": {
    "supabase": "^1.200.3",
    "turbo": "^2.1.0",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@9.4.0"
}
